const AddComments = () => {

    const onAddComment = () => {

     };

  return (
    <section>
      <textarea rows={4} cols={4}></textarea>
      <button onClick={onAddComment}>Add</button>
    </section>
  );
};

export default AddComments;
